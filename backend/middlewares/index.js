const product_middleware = require('./product.middleware');
const registro_middleware = require('./registro.middleware');
const venta_middleware = require('./venta.middleware');

module.exports = Object.freeze({
    product_middleware,registro_middleware,venta_middleware
})